import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'native-list-view',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
